{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('profile', 'edit') }}">Edit</a>
    <img src="{{ user.picturePath }}" alt="Profile Picture" width="150" height="150">
    <h1>{{ user }}</h1>
    <h2>{{ user.email }}</h2>
{% endblock %}